{
  "name": "lewahub",
  "private": true,
  "scripts": {
    "build": "cd Backend && npm install && npx prisma generate",
    "start": "cd Backend && npm start"
  },
  "engines": {
    "node": "20.x"
  }
}